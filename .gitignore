# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
.next/
out/
build/

# Environment variables — NE JAMAIS committer
.env.local
.env.*.local

# GUYSKULL-04A — local-only rollback evidence (may contain establishment
# baseline field values); never committed, generated fresh by each run.
docs/guyskull/scripts/*.local.json

# TypeScript
tsconfig.tsbuildinfo
next-env.d.ts

# Misc
.DS_Store
*.pem
.claude/

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Registry scripts — scratch/one-off working files only (SPRINT R.2-SAFETY-CLOSE §27).
# Durable artifacts belong in data/registry/raw/, reports/registry/, or scripts/school-registry/
# itself as a named reusable script — never here.
scripts/school-registry/tmp/

# Supabase CLI local state (SPRINT REGISTRY-MULTI-B) — version cache only, not a project artifact.
supabase/.temp/
