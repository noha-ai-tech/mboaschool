# 03 — Feature Status

| Fonctionnalité | État | Preuve | Limites | Prochaine étape |
|---|---|---|---|---|
| Annuaire public (recherche, filtres, catégories) | FONCTIONNEL | `src/app/page.tsx` — requête Supabase réelle sur `establishments` avec jointures `fees`, `infrastructures`, `school_images` | Recherche textuelle côté client uniquement (pas de recherche plein texte serveur) ; pas de pagination (tout est chargé puis filtré en mémoire) | Vérifier le comportement à l'échelle (>200 écoles) |
| Géolocalisation / carte | FONCTIONNEL | `src/components/LocalSchoolMap.tsx` (Leaflet + OpenStreetMap, pas de clé API requise), calcul de distance en JS pur (`haversineKm` dans `page.tsx`) | Dépend de `navigator.geolocation` (comportement navigateur, pas de fallback serveur) ; tuiles OpenStreetMap publiques (pas de SLA) | Aucune action bloquante identifiée |
| Fiche publique d'établissement | FONCTIONNEL | `src/app/ecole/[id]/page.tsx` — carrousel, frais, infrastructures, documents, annonces, onglet "Espace parent" | L'onglet "Espace parent" est un écran d'annonce de fonctionnalité future, pas un vrai espace ; `school.subscription_plan` et badges premium affichés mais jamais mis à jour par un flux de paiement réel | Décider si l'onglet "Espace parent" doit être masqué tant qu'il n'existe pas |
| Création de compte école | PARTIEL | `src/app/auth/inscription/page.tsx` (Auth uniquement) + `src/app/dashboard/ecole/onboarding/page.tsx` (création d'établissement séparée) | Aucune vérification qu'un `owner_id` ne possède pas déjà un établissement avant d'en créer un nouveau (risque de duplication) ; aucune validation manuelle avant publication mentionnée dans le code (le texte affiché — "visible après vérification par l'équipe" — n'est adossé à aucun état `pending` dans le schéma connu) | Ajouter une contrainte d'unicité applicative ou base, et un état de vérification explicite si le texte utilisateur doit rester vrai |
| Revendication d'une fiche existante | **PRÉSENT MAIS NON CONNECTÉ** | Bouton dans `src/app/page.tsx` (`Revendiquer cette page` → `/auth/inscription?ecole=${school.id}`) ; `auth/inscription/page.tsx` ne lit jamais `useSearchParams`/le paramètre `ecole` | La colonne `is_claimed` existe (ajoutée par `supabase/seed_schools.sql`) mais n'est modifiée par aucun code applicatif trouvé | Construire le flux réel : lier la fiche existante au nouveau compte au lieu d'en créer une nouvelle, avec vérification (email pro, document, ou validation admin) |
| Dashboard école — vue d'ensemble | FONCTIONNEL | `src/app/dashboard/ecole/page.tsx` — stats calculées depuis `applications` et `classes` réels | Bannière "Module Pro" conditionnée par `school.forfait` — cohérent avec le middleware | Aucune |
| Dashboard école — admissions | FONCTIONNEL | `src/app/dashboard/ecole/admissions/page.tsx` — changement de statut, notes internes, tiroir de détail | Dépend de colonnes non tracées dans les migrations (`student_first_name`, `desired_level`, `notes`…) | Documenter/migrer le schéma réel de `applications` |
| Dashboard école — classes | FONCTIONNEL | `src/app/dashboard/ecole/classes/*` — CRUD complet | Isolation par établissement correcte (`establishment_id` filtré + policy RLS `Owners can manage classes`) | Aucune |
| Dashboard école — galerie / documents | FONCTIONNEL | Upload réel vers Supabase Storage (`school-images`, `school-documents`) avec insertion en base | Politique de stockage décrite uniquement en commentaire dans `auth-setup.sql` ("à créer via le dashboard") — NON VÉRIFIABLE si appliquée en production | Confirmer la configuration réelle des buckets et de leurs policies |
| Dashboard école — frais / infrastructures | FONCTIONNEL | Upsert manuel (`fees`, `infrastructures`) par établissement | Un seul enregistrement par table et par établissement (`maybeSingle`) — cohérent avec le schéma | Aucune |
| Dashboard école — paiements | **ABSENT** (placeholder) | `src/app/dashboard/ecole/paiements/page.tsx` — écran statique "Prochainement" | Aucune logique, aucune table `payments` connectée bien que `schema.sql` déclare une table `payments` | Ne pas confondre avec "fonctionnel" dans toute communication externe |
| Pré-inscriptions (formulaire parent) | PARTIEL | `src/app/preinscription/page.tsx` — insertion réelle dans `applications` | N'utilise pas la table/le flux `pre_inscriptions` avec code de suivi décrit dans `CLAUDE_CONTEXT.md` — ce flux planifié n'a jamais été implémenté sous cette forme ; le flux actuel existe mais sous un nom et une structure différents | Clarifier avec le fondateur si `pre_inscriptions` (avec code de suivi public) est toujours souhaité en plus de/à la place de `applications` |
| Administration plateforme — liste | FONCTIONNEL | `src/app/dashboard/admin/page.tsx` — lecture réelle, recherche, stats | Bouton "Ajouter" un établissement visible mais sans `onClick` fonctionnel (aucun formulaire ni route associée trouvée) | Retirer ou implémenter |
| Administration plateforme — édition d'une école | **PARTIEL / NON VÉRIFIABLE** | `src/app/dashboard/admin/ecoles/[id]/page.tsx` — formulaire complet (plan, vérification, mise en avant) | Écrit sur `establishments` avec le client anonyme ; aucune policy RLS `UPDATE` pour `platform_admin` trouvée dans le dépôt (seule policy connue : `owner_id = auth.uid()`) | Vérifier en environnement réel ; si RLS bloque, soit ajouter une policy `platform_admin`, soit faire passer cette route par une API serveur avec le client service role |
| Plans commerciaux (Autonome / Gérée / Pro) | PARTIEL | Colonne `establishments.forfait` (`gratuit`/`gere`/`pro`, migration `0005`) pilote réellement l'accès au module Pro via le middleware | Les colonnes `plan_type` et `module_pro_actif` décrites dans `CLAUDE_CONTEXT.md` **n'existent pas** — le modèle réellement implémenté utilise `forfait`, un concept voisin mais distinct et non synchronisé avec la documentation. L'offre "Gérée" n'a aucune logique dédiée trouvée (pas d'accès admin délégué, pas de journal des modifications) | Aligner la documentation produit sur le nom de colonne réel (`forfait`) ou migrer vers le modèle documenté, mais pas les deux en parallèle |
| Module Pro — emplois du temps | FONCTIONNEL | `src/app/pro/emplois-du-temps/page.tsx`, `src/lib/timetable/*`, migration `0001_timetable_schema.sql`, API `POST /api/timetable/generate` | Algorithme de génération non audité ligne à ligne dans cet audit (hors périmètre "ne pas développer") | Revue dédiée de l'algorithme si des anomalies de planning sont rapportées |
| Module Pro — pointage enseignants | FONCTIONNEL | Migration `0002_presence.sql`, `pro/pointage/kiosque` (capture photo caméra), API `/api/pointage/enregistrer`, RPC `calculer_heures_enseignant` | Bucket `pointages-photos` privé avec policy dédiée (`pointages_owner_access`) — cohérent | Revalider le modèle d'accès du kiosque (voir `02_ROUTE_INVENTORY.md`) |
| Module Pro — comptes enseignants / invitations | FONCTIONNEL | Migration `0003_comptes_enseignants.sql`, `api/enseignants/creer`, `api/enseignants/[id]/inviter`, `auth/enseignant-bienvenue` | Utilise correctement le client service role côté serveur uniquement | Aucune |
| Module Pro — messagerie interne | FONCTIONNEL | Migration `0004_messagerie.sql`, `pro/messagerie`, `api/messagerie/envoyer` | Diffusion globale ou par département — RLS dédiée et cohérente avec le rôle enseignant | Aucune |
| Module Pro — sections / responsables de section | **ABSENT** | Aucune table `sections`, aucune référence dans le code | Uniquement mentionné comme anticipation future dans `CLAUDE_CONTEXT.md` | Confirmer si toujours dans la feuille de route avant de concevoir |
| Espace enseignant multi-établissement | FONCTIONNEL | `src/app/enseignant/mon-espace/page.tsx`, `SelecteurEtablissement.tsx`, migration `0005_forfait_multi_etab.sql` | Protection par vérification serveur locale, pas par middleware (incohérence architecturale, pas nécessairement une faille) | Documenter ce choix ou aligner sur le middleware |
| Changement de nom MboaSchool → Écoles237 | **FONCTIONNEL (déjà effectué)** | Recherche exhaustive : aucune occurrence de "MboaSchool" dans le code, la configuration ou les scripts SQL du dépôt | `package.json` conserve le nom interne `ecoles237-mvp` (cohérent, pas un résidu de l'ancien nom) | Voir `10_RENAME_MBOASCHOOL_TO_ECOLES237.md` pour la nuance sur les données Supabase elles-mêmes (NON VÉRIFIABLE depuis ce dépôt) |
